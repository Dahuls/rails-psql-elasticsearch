production:
    last_thing:
        command: cd /usr/share/elasticsearch/bin && plugin -u https://s3.amazonaws.com/www.emergi.net/elasticsearch/plugins/elasticsearch-jetty-1.0.0.zip -i elasticsearch-jetty-1.0.0
        target: elasticsearch
        apply_during: build_only
        command: echo "http.type: com.sonian.elasticsearch.http.jetty.JettyHttpServerTransportModule" >> /etc/elasticsearch/elasticsearch.yml
        target: elasticsearch
        apply_during: build_only
        command: sudo service elasticsearch restart
        target: elasticsearch
        apply_during: build_only
